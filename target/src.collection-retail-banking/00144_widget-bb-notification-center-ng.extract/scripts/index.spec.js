import widget from './index';

describe('Widget Notifications Center', function() {
  it('should export the module name', function() {
    expect(widget).toEqual('widget-bb-notification-center-ng');
  });
});
